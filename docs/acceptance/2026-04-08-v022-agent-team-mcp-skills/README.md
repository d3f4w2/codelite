# v0.2.2 Agent Team + MCP + Skills Compatibility Acceptance Bundle

日期：2026-04-08

本阶段新增并纳入人工验收的机制：

- Agent Team 运行时（team create/list）
- Subagent 运行时（spawn/process/list/show，含队列路径）
- Skills 外部兼容加载（支持 `SKILL.md` 目录，拿来即用）
- MCP 入口（mcp add/list/call/remove）
- ToolRouter 新工具面（`skills_list/subagent_*/mcp_*`）

验收文档：

- `manual-commands.md`

产物目录：

- `artifacts/command-output/`
- `artifacts/runtime/`
